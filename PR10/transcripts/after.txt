$ git checkout ci/skip-preview-for-forks
$ grep -c 'head.repo.full_name == github.repository' .github/workflows/deploy-preview.yml
7        <- every preview job now gated to same-repo PRs

$ actionlint .github/workflows/deploy-preview.yml
✓ 0 problems — workflow valid

$ # evaluate the guard condition (github.event.pull_request.head.repo.full_name == github.repository):
  fork PR       head.repo.full_name=DivyamTalwar/superset      == superset-sh/superset  ->  False  ->  SKIP — no secret access
  same-repo PR  head.repo.full_name=superset-sh/superset       == superset-sh/superset  ->  True   ->  RUN (trusted)
